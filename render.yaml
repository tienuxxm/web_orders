services:
  - type: web
    name: laravel-api
    env: docker
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_KEY
        sync: false
      - key: DB_CONNECTION
        value: mysql
      # Thêm các biến môi trường khác từ .env của bạn
